#!/usr/bin/env bash
set -euo pipefail

if [ -z "${DATA_DIR:-}" ]; then
  echo "ERROR: DATA_DIR is not set. Are you in the dev shell? (nix develop)" >&2
  exit 1
fi

exec process-compose up -f "$DATA_DIR/process-compose.yaml" -u "$PC_SOCKET"
